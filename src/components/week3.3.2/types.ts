export type Client = {
    id: string; // ID klienta
    firstName: string; // Imię klienta
    lastName: string; // Nazwisko klienta
    street: string; // Ulica
    postalCode: string; // Kod pocztowy
    city: string; // Miasto
    region: string; // Region
    photoUrl: string; // URL do zdjęcia
    phoneNumber: string; // Numer telefonu
  }